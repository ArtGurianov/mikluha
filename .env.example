# production | staging — controls indexability/canonical URLs (PRD section 44).
DEPLOY_ENV=production

# Only used when DEPLOY_ENV=staging: overrides the canonical/OG base URL so
# staging builds don't leak the production domain. See lib/site.ts.
SITE_URL=

# Content lives in content/ (this repo), edited via /admin (Sveltia CMS) or by
# hand — no CMS credentials are needed to build. See RUNBOOK.md for the CMS
# admin's own setup (GitHub OAuth App + oauth-broker + cloud.ru bucket).
